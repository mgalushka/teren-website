AuthUserFile .htpasswd
AuthGroupFile /dev/null
AuthName "BPG project secure directory"
AuthType Basic

<Limit GET POST>
require valid-user
</Limit>

<IfModule mod_rewrite.c>
   RewriteEngine on
   RewriteRule    ^$ app/webroot/    [L]
   RewriteRule    (.*) app/webroot/$1 [L]
</IfModule>